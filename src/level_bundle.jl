# Level bundle method for solving the master problem

function solve_master_level(
        master::MasterProblem,
        recourse::RecourseData;
        max_iter::Int = NUM_MAX_ITER,
        opt_gap::Float64 = VAL_TOL
    )
    # TODO: implement the level method
end
